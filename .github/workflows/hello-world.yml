name: Hello World

on:
  push:
  pull_request:
  schedule:
    - cron: "*/1 * * * *" # every minute

runs-on: ubuntu-latest

jobs:
  first-job:
    name: Say hello
    steps:
      - name: Show ASCII greeting
        uses: mscoutermarsh/ascii-art-action@master
        with:
          text: "Puru V!"
