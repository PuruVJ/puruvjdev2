name: Hello World

on:
  push:
  pull_request:
  schedule:
    - cron: "*/1 * * * *" # every minute

jobs:
  first-job:
    runs-on: ubuntu-latest
    name: Say hello
    steps:
      - name: Print a greeting
        run: echo "Hi from beyond"
