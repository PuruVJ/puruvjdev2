name: Hello World

on:
  push:
  pull_request:
  schedule:
    - cron: "*/1 * * * *" # every minute

jobs:
  first-job:
    runs-on: ubuntu-latest
    name: Say hello
    steps:
      - name: Show ASCII greeting
        uses: mscoutermarsh/ascii-art-action@master
        with:
          text: "Puru V!"
